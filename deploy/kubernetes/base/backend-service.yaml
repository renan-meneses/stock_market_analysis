apiVersion: v1
kind: Service
metadata:
  name: financial-api
  namespace: financial-dashboard
  labels:
    app: financial-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: financial-api
